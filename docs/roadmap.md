# ロードマップ

PDFto の計画を一元管理する。ロードマップは複数の**マイルストーン**で構成し、
各マイルストーンは複数の **issue**（具体的な作業単位）を持つ。
進め方の方針は [../CLAUDE.md](../CLAUDE.md) を参照。

**状態の凡例:** `todo` / `in-progress` / `done`
各 issue は、振る舞いを変える場合 `docs/specs/` にスペックを持つ。

---

## M0: MVP（最小の動くプロダクト） — `done`

**目的:** PDF をアップロードし、内容に応じた質問に答えて変換し、ダウンロードできる、
API ファーストの最小構成を成立させる。

**完了条件 (DoD):**
- アップロード → 質問 → 変換 → ダウンロードが API と Web UI の両方で動く。
- docling による実変換が成功する。
- テストが通る。

| issue | 内容 | 状態 | spec |
|------|------|------|------|
| M0-1 | PDF 軽量解析（pypdf）と質問生成 | done | — |
| M0-2 | docling 変換コア（形式/OCR/表/画像/ページ範囲） | done | — |
| M0-3 | REST API（対話フロー + ワンショット）+ OpenAPI | done | — |
| M0-4 | Web UI（アップロード→質問→ダウンロード） | done | — |
| M0-5 | テスト（解析/質問/API、docling モック） | done | — |

---

## M1: 信頼性と運用性 — `done`

**目的:** 単発スクリプトではなく、安定して運用できるサービスにする。

**完了条件 (DoD):**
- 大きい/重い PDF でもリクエストがタイムアウトせず進捗が分かる。
- 保存物に有効期限があり、ディスクが無限に増えない。
- コンテナで再現性よく起動できる。

| issue | 内容 | 状態 | spec |
|------|------|------|------|
| M1-1 | 非同期ジョブ化（変換をバックグラウンド処理 + 進捗ポーリング） | done | [0001](specs/0001-async-jobs.md) |
| M1-2 | 保存物の TTL と自動削除（`data/` の掃除） | done | [0002](specs/0002-ttl-cleanup.md) |
| M1-3 | Docker 化（モデルを焼き込み、初回 DL を不要に） | done | [0003](specs/0003-docker.md) |
| M1-4 | 構造化ログとエラーハンドリングの整備 | done | [0004](specs/0004-logging-errors.md) |

---

## M1.5: 状態の永続化 — `done`

**目的:** ドキュメント索引とジョブをプロセス再起動で失わないようにし、M2（認証・
レート制限・連携）の前提となる「状態の永続化」を整える。

**完了条件 (DoD):**
- 再起動後もアップロード済みドキュメント・変換成果物・完了ジョブを参照できる。
- 中断したジョブ（再起動で消えたスレッド分）が整合的に扱われる。
- 索引アクセスがリポジトリ抽象越しになり、将来 Postgres 等へ差し替えやすい。

| issue | 内容 | 状態 | spec |
|------|------|------|------|
| M1.5-1 | SQLite による索引/ジョブの永続化（再起動で状態保持） | done | [0005](specs/0005-persistence.md) |
| M1.5-2 | 再起動時の中断ジョブ復旧（pending/running → failed） | done | [0005](specs/0005-persistence.md) |

---

## M2: 公開・連携の強化 — `todo`

**目的:** 他システムからより安全・便利に使えるようにする。

**完了条件 (DoD):**
- 認証とレート制限があり、外部公開に耐える。
- 他システムが組み込みやすいクライアント手段がある。

| issue | 内容 | 状態 | spec |
|------|------|------|------|
| M2-1 | API キー認証 + レート制限 | todo | — |
| M2-2 | Webhook / コールバック（変換完了通知） | todo | — |
| M2-3 | バッチ変換（複数 PDF を一括投入） | todo | — |
| M2-4 | クライアント SDK もしくは OpenAPI からの生成手順 | todo | — |

---

## M3: 変換品質 — `todo`

**目的:** 出力の品質と扱える文書の幅を広げる。

**完了条件 (DoD):**
- 表・レイアウト・多言語 OCR の品質が実用文書で確認できる。

| issue | 内容 | 状態 | spec |
|------|------|------|------|
| M3-1 | OCR 言語の選択と多言語対応 | todo | — |
| M3-2 | 表抽出の品質改善・検証用サンプル整備 | todo | — |
| M3-3 | 画像の参照モード（別ファイル書き出し）の API/UI 完成 | todo | — |
| M3-4 | LLM による任意整形（オプション、API キー前提） | todo | — |

---

## ふりかえりログ

マイルストーン完了ごとに「うまくいったこと / 課題 / 次に変えること」を記録する。
ここで決めたプロセス改善は CLAUDE.md に反映する。

### M0（MVP）
- **うまくいったこと:** コア（解析/質問/変換）を Web 層から分離できた。docling を
  遅延 import にしたことで、モデル無しでもテストが回る構成にできた。
- **課題:** 保存がインメモリ索引で、再起動で消える。変換が同期実行で重い PDF に弱い。
- **次に変えること:** 信頼性・運用性（M1）を次の焦点にする。振る舞いを変える issue は
  着手前にスペックを書く運用を M1 から徹底する。

### M1（信頼性と運用性）
- **うまくいったこと:** スペック駆動（spec → 合意 → 実装 → 検証 → 記録）を 4 issue で
  一貫して回せた。非同期化・TTL・Docker・ログ/エラーで運用性が大きく前進。Docker は
  `--network none` でオフライン変換を実証し、受け入れ条件を実機検証できた。
- **課題:** ジョブ索引・ストレージ索引が依然インメモリで、プロセス再起動やマルチ
  インスタンスに弱い（永続化は未対応）。Docker イメージが約 4.3GB と大きい。
  サンドボックスのプロキシ CA でコンテナビルドがハマり、検証に回り道した。
- **次に変えること:** 永続化（DB/オブジェクトストレージ）とイメージ軽量化は将来の
  issue として明示的に積む。M2（公開・連携）に進む前に、認証/レート制限の前提と
  なる「インスタンスをまたぐ状態共有」をどう扱うかを M2 スペックで先に決める。

### M1.5（状態の永続化）
- **うまくいったこと:** `Database` を薄い抽象として切り出し、`Storage` と
  `JobManager` を SQLite 化しても公開 API はほぼ不変に保てた。再起動を跨ぐ
  ドキュメント参照を実機で確認し、中断ジョブ復旧もユニットテストで担保。
  既存テストは `JobManager(db=...)` への小改修で済んだ。
- **課題:** 単一コネクション + Lock のため高負荷では直列化がボトルネックになり得る。
  ジョブ実行は依然プロセス内（マルチインスタンスでの分散実行は未対応）。
- **次に変えること:** M2 では「ジョブを投入したインスタンスが実行する」前提を
  明記して認証・レート制限を設計する。分散実行（共有キュー）が必要になった時点で
  別マイルストーン化し、`Database` 抽象を Postgres 等へ差し替える。
